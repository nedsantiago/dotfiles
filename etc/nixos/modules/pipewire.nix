{ config, pkgs, ... }:

{
    # # Pipewire for audio
    # services.pipewire = {
    #     enable = true;
    #     pulse.enable = true;
    #     alsa.enable = true;
    #     jack.enable = true;
    # };

    # Pipewire for audio<D-2>
}
